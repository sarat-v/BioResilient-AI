[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    slow: marks tests that run the full 20-species integration pipeline (run manually before phase completion)
python_files = test_*.py
python_classes = Test*
python_functions = test_*
